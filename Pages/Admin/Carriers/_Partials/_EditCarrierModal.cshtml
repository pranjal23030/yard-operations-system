@using YardOps.Pages.Admin.Carriers
@model IndexModel

<div class="modal fade" id="editCarrierModal" tabindex="-1" aria-labelledby="editCarrierModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content modal-custom">
            <div class="modal-header modal-header-custom">
                <h5 class="modal-title modal-title-custom" id="editCarrierModalLabel">Edit Carrier</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <form method="post" asp-page-handler="Edit">
                <input type="hidden" asp-for="EditInput.CarrierId" id="editCarrierId" />
                <input type="hidden" asp-for="EditInput.CarrierCode" id="editCarrierCode" />
                <div class="modal-body modal-body-custom">
                    @if (!ViewData.ModelState.IsValid && Model.ShowEditModal)
                    {
                        <div class="alert alert-danger" role="alert">
                            <i class="fa-solid fa-exclamation-circle me-2"></i>
                            <strong>Please fix the following errors:</strong>
                            <ul class="mb-0 mt-2">
                                @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                                {
                                    <li>@error.ErrorMessage</li>
                                }
                            </ul>
                        </div>
                    }

                    <div class="edit-carrier-code-display">
                        <span class="label">Carrier ID:</span>
                        <span class="value" id="editCarrierCodeDisplay"></span>
                    </div>

                    <div class="form-group-custom">
                        <label asp-for="EditInput.CompanyName" class="form-label-custom">Company Name *</label>
                        <input asp-for="EditInput.CompanyName" id="editCompanyName" class="form-input-custom" placeholder="e.g., Nepal Freight Lines" />
                        <span asp-validation-for="EditInput.CompanyName" class="validation-error-custom"></span>
                    </div>

                    <div class="form-group-custom">
                        <label asp-for="EditInput.ContactPerson" class="form-label-custom">Contact Person *</label>
                        <input asp-for="EditInput.ContactPerson" id="editContactPerson" class="form-input-custom" placeholder="e.g., Ram Shrestha" />
                        <span asp-validation-for="EditInput.ContactPerson" class="validation-error-custom"></span>
                    </div>

                    <div class="row g-3">
                        <div class="col-md-6">
                            <div class="form-group-custom">
                                <label asp-for="EditInput.Phone" class="form-label-custom">Phone</label>
                                <input asp-for="EditInput.Phone" id="editPhone" class="form-input-custom" placeholder="+977 9841234567" />
                                <span asp-validation-for="EditInput.Phone" class="validation-error-custom"></span>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group-custom">
                                <label asp-for="EditInput.Email" class="form-label-custom">Email</label>
                                <input asp-for="EditInput.Email" id="editEmail" type="email" class="form-input-custom" placeholder="contact@company.com" />
                                <span asp-validation-for="EditInput.Email" class="validation-error-custom"></span>
                            </div>
                        </div>
                    </div>

                    <div class="form-group-custom">
                        <label asp-for="EditInput.Address" class="form-label-custom">Address</label>
                        <textarea asp-for="EditInput.Address" id="editAddress" class="form-input-custom" rows="2" placeholder="Company address..."></textarea>
                        <span asp-validation-for="EditInput.Address" class="validation-error-custom"></span>
                    </div>

                    <div class="form-group-custom">
                        <label asp-for="EditInput.Status" class="form-label-custom">Status</label>
                        <select asp-for="EditInput.Status" id="editStatus" class="form-select-custom">
                            <option value="Active">Active</option>
                            <option value="Inactive">Inactive</option>
                        </select>
                        <span asp-validation-for="EditInput.Status" class="validation-error-custom"></span>
                    </div>
                </div>
                <div class="modal-footer modal-footer-custom">
                    <button type="button" class="btn-cancel-neutral" data-bs-dismiss="modal">Cancel</button>
                    <button type="submit" class="btn-primary-custom">Save Changes</button>
                </div>
            </form>
        </div>
    </div>
</div>